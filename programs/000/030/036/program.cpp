#include <iostream>

using namespace std;

int main() {
	// see https://en.cppreference.com/w/cpp/language/storage_duration

	/**
	 * Storage class specifiers
	 * C++ language Declarations
	 * The storage class specifiers are a part of the
	 * decl-specifier-seq of a name's declaration syntax.
	 * 
	 * Together with the scope of the name, they control
	 * two independent properties of the name: its storage
	 * duration and its linkage.
	 */
	cin.get();

	// extern

	// extern - static or thread storage duration and external linkage.

	return 0;
}